{
  "name": "babytato-link",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host --port 4322 --strictPort",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "wrangler pages dev dist --port 8788 --ip 127.0.0.1 --strictPort",
    "pages:deploy": "rm -rf node_modules/.cache/wrangler 2>/dev/null; wrangler pages deploy dist --project-name=babytato-link --branch=main --commit-dirty=true",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.40.0"
  },
  "engines": {
    "node": ">=20"
  }
}
