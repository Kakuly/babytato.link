{
  "name": "babytato-link",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host --port 4322 --strictPort",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "wrangler pages dev dist",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.40.0"
  },
  "engines": {
    "node": ">=20"
  }
}
